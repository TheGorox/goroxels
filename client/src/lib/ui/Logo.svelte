<div class="logo">
    GOROXELS 
    <span class="logoEmoji">☀️</span>
</div>

<style>
    .logo {
        position: fixed;
        top: 9px;
        left: 9px;

        font-family: BoldPixels;
        color: var(--accent);
        font-size: 16px;
        text-shadow: 
            2px  0   0  var(--bg-dark), 
            -2px  0   0 var(--bg-dark), 
            0    2px 0  var(--bg-dark), 
            0   -2px 0 var(--bg-dark),
            2px  2px 0 var(--bg-dark), 
            -2px -2px 0 var(--bg-dark), 
            2px -2px 0 var(--bg-dark), 
            -2px  2px 0 var(--bg-dark);
    }

    .logoEmoji {
        font-size: var(--text-xs);
        text-shadow: none;
        margin-left: -5px;
    }
</style>